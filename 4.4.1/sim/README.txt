In order to simulate the Micro CU using our script follow these steps:
1. Launch questaSim
2. Within the QuestaSim's console run "source simulation_CU_UP.scr" to compile and simulate the Micro CU


!!! In order to guarantee a correct behavior the input signals (opcode and func) must to stay stable over all 3 clock periods.
